// Importing this file registers all built-in search providers.
// Import once at the search engine entry point (e.g. the command palette or search UI).

import "./command.provider";
import "./crm.provider";
